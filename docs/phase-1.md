# 第一阶段：页面预览

唯一有效产品规格为用户提供的 Product Spec.docx 中 PROJECT_SPEC V5.0 / FINAL，叠加用户确认的决定：仅浅色、无评分、保留搜索。Mockup 只用于视觉。技术栈使用 React、Vite、TypeScript、CSS、JSON、localStorage；不引入 UI 框架、状态管理框架或后端。

本次交付范围：页面骨架、首页四入口、基础导航、字号（默认大）、本地字号保存、页面名称搜索、参考表分类切换、设置和说明页。内容区明确标为待整理。仅有已在首次检查中核对的功能名称，不录入操作参数或菜谱。

尚未交付：批量官方内容、完整搜索、真实内容收藏、步骤模式、PWA 安装与离线缓存、线上部署。本轮按用户最新指示优先交付实际视觉页面，以上不作为已完成功能。

## 检查清单
- 首页四入口、底部导航、返回与搜索入口可达。
- 字号默认大，切换后本地保存，存储不可用时提示。
- 320px 手机与平板布局，特大字号和键盘焦点。
- 搜索页面名/中英文功能名，空结果明确。
- 不展示 Mockup 评分、虚构官方数据或深色模式。
- 第一阶段用户检查后再继续，不自动进入第二阶段。
